import 'package:flutter/material.dart';

class AIInterviewsView extends StatelessWidget {
  const AIInterviewsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
